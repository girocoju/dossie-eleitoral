# Layout TSE 1998 — herda tse_base.yml e sobrescreve so' o que muda.
# `verificado: false` significa: o header real ainda NAO foi conferido contra este
# arquivo nesta maquina. Rode `python -m ingest.tse verify-layout --ano 1998`
# e so' entao mude para true (SPEC §9: nao adivinhar layout).
extends: tse_base.yml
ano: 1998
verificado: false
notas: |
  Ano mais antigo do escopo (SPEC §2.1). Alta chance de campos opcionais vazios;
  registrar o que faltar em docs/LACUNAS.md, nunca preencher.
