# Layout TSE 2006 — herda tse_base.yml e sobrescreve so' o que muda.
# `verificado: true` significa: o header real ainda NAO foi conferido contra este
# arquivo nesta maquina. Rode `python -m ingest.tse verify-layout --ano 2006`
# e so' entao mude para true (SPEC §9: nao adivinhar layout).
extends: tse_base.yml
ano: 2006
verificado: true
notas: |
  CONFERIDO em 27/08/2026: 28 arquivos por UE, 52 de 56 campos (so' faltam os de
  federacao). Primeiro ano com declaracao de bens publicada pelo TSE.
