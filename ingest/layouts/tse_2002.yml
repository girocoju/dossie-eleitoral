# Layout TSE 2002 — herda tse_base.yml e sobrescreve so' o que muda.
# `verificado: false` significa: o header real ainda NAO foi conferido contra este
# arquivo nesta maquina. Rode `python -m ingest.tse verify-layout --ano 2002`
# e so' entao mude para true (SPEC §9: nao adivinhar layout).
extends: tse_base.yml
ano: 2002
verificado: false
notas: |
  Arquivo historico reprocessado pelo TSE. Idem 2006 quanto ao CPF.
